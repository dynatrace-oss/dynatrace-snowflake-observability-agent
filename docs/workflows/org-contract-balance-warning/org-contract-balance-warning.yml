# WORKFLOW: DSOA — Org Contract Balance Warning
# DESCRIPTION: Monitors organization-level remaining contract balance and alerts when any balance
#              category drops below a configurable threshold
# OWNER: DSOA Team
# PLUGINS: org_costs
# TAGS: snowflake, dsoa, operations, costs, billing, org, finops

id: b3f2a1e4-7c8d-4e9f-a0b1-c2d3e4f5a6b7
title: "DSOA — Org Contract Balance Warning"
description: |
  Monitors organization-level remaining Snowflake contract balances (free usage, capacity,
  on-demand, rollover, and total) and sends an alert when any balance drops below a threshold.
  Requires the org_costs plugin with the org_billing_remaining_balance context enabled and
  access to SNOWFLAKE.ORGANIZATION_USAGE.
isPrivate: false
schemaVersion: 4
trigger:
  schedule:
    isActive: true
    trigger:
      type: interval
      intervalMinutes: 360
    rule: null
    filterParameters: {}
    timezone: UTC
    inputs: {}
tasks:
  fetch_balances:
    name: fetch_balances
    description: Query remaining org contract balances via DQL timeseries
    action: dynatrace.automations:execute-dql-query
    active: true
    position:
      x: 0
      y: 1
    conditions:
      states: {}
    predecessors: []
    input:
      query: |
        timeseries {
          free_usage_balance     = avg(snowflake.org.billing.free_usage_balance),
          capacity_balance       = avg(snowflake.org.billing.capacity_balance),
          on_demand_consumption  = avg(snowflake.org.billing.on_demand_consumption),
          rollover_balance       = avg(snowflake.org.billing.rollover_balance),
          overage                = avg(snowflake.org.billing.overage)
        }
        , from: now()-7d
        , by: { deployment.environment }

  check_balance:
    name: check_balance
    description: Evaluate balance values and build alert list if below threshold
    action: dynatrace.automations:run-javascript
    active: true
    position:
      x: 0
      y: 2
    conditions:
      states:
        fetch_balances: SUCCESS
    predecessors:
      - fetch_balances
    input:
      script: |
        import { execution } from '@dynatrace-sdk/automation-utils';

        // Threshold below which to trigger an alert (in USD or contract currency)
        const BALANCE_THRESHOLD = 1000;
        const METRIC_FIELDS = [
          'free_usage_balance',
          'capacity_balance',
          'on_demand_consumption',
          'rollover_balance',
          'overage',
        ];
        const METRIC_IDS = {
          free_usage_balance:    'snowflake.org.billing.free_usage_balance',
          capacity_balance:      'snowflake.org.billing.capacity_balance',
          on_demand_consumption: 'snowflake.org.billing.on_demand_consumption',
          rollover_balance:      'snowflake.org.billing.rollover_balance',
          overage:               'snowflake.org.billing.overage',
        };

        export default async function ({ execution_id }) {
          const ex = await execution(execution_id);
          const dqlResult = await ex.result('fetch_balances');
          const records = dqlResult?.records ?? [];

          const alerts = [];

          for (const record of records) {
            const env = record['deployment.environment'] ?? '';
            for (const field of METRIC_FIELDS) {
              const series = record[field] ?? [];
              // series is an array of values — take the last non-null value
              const latestValue = [...series].reverse().find((v) => v !== null) ?? null;
              if (latestValue !== null && latestValue < BALANCE_THRESHOLD) {
                alerts.push({
                  metric: METRIC_IDS[field],
                  value: latestValue,
                  threshold: BALANCE_THRESHOLD,
                  dimensions: { 'deployment.environment': env },
                });
              }
            }
          }

          if (alerts.length > 0) {
            console.log(`CONTRACT BALANCE ALERT: ${alerts.length} metric(s) below threshold ${BALANCE_THRESHOLD}`);
          } else {
            console.log('All org contract balances are above threshold.');
          }

          return { hasAlerts: alerts.length > 0, alerts };
        }

  ingest_balance_events:
    name: ingest_balance_events
    description: Ingest a Dynatrace event for each org contract balance that dropped below threshold
    action: dynatrace.automations:run-javascript
    active: true
    position:
      x: 0
      y: 3
    conditions:
      states:
        check_balance: OK
      custom: "{{ result('check_balance').hasAlerts == true }}"
    predecessors:
      - check_balance
    input:
      script: |
        import { execution } from '@dynatrace-sdk/automation-utils';
        import { eventsClient } from '@dynatrace-sdk/client-classic-environment-v2';

        export default async function ({ execution_id }) {
          const ex = await execution(execution_id);
          const { alerts } = await ex.result('check_balance');

          let successCount = 0;
          const failed = [];
          for (const a of alerts) {
            const dimProps = Object.fromEntries(
              Object.entries(a.dimensions).map(([k, v]) => [k, String(v)])
            );
            const eventBody = {
              body: {
                eventType: 'CUSTOM_ALERT',
                title: `Org contract balance below threshold: ${a.metric}`,
                timeout: 360,
                properties: {
                  'event.description':
                    `Balance for ${a.metric} is ${a.value.toFixed(2)}, ` +
                    `below threshold ${a.threshold}.`,
                  'anomaly.detector': 'dsoa.org_contract_balance_warning',
                  'metric.key': a.metric,
                  'balance.value': String(a.value.toFixed(2)),
                  'balance.threshold': String(a.threshold),
                  ...dimProps,
                },
              },
            };
            const res = await eventsClient.createEvent(eventBody);
            if (res.reportCount !== 1) {
              failed.push({ reason: 'Invalid number of ingested events', metric: a.metric });
            } else {
              const status = res.eventIngestResults[0];
              if (status.status === 'OK') {
                successCount += 1;
              } else {
                failed.push({ status: JSON.stringify(status), metric: a.metric });
              }
            }
          }
          if (successCount > 0) console.log(`Successfully ingested ${successCount} event(s)`);
          if (failed.length > 0) console.log(`Failed to ingest ${failed.length} event(s):`, JSON.stringify(failed));
          return alerts;
        }
