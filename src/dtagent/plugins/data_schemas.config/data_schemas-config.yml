plugins:
  data_schemas:
    lookback_hours: 4
    schedule: USING CRON 0 0,8,16 * * * UTC
    is_disabled: false
    exclude: []
    include:
      - '%'
    telemetry:
      - events
      - biz_events
