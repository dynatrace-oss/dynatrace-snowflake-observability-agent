plugins:
  dynamic_tables:
    include:
      - '%.%.%'
    exclude:
      - DTAGENT_DB.%.%
    schedule: USING CRON */30 * * * * UTC
    schedule_grants: USING CRON 30 */12 * * * UTC
    is_disabled: false
    telemetry:
      - metrics
      - logs
      - biz_events
