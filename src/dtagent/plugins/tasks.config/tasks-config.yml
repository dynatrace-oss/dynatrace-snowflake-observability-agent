plugins:
  tasks:
    lookback_hours: 4
    lookback_hours_versions: 720
    schedule: USING CRON 30 * * * * UTC
    is_disabled: false
    telemetry:
      - logs
      - metrics
      - events
      - biz_events
