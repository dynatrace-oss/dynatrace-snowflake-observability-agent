plugins:
  event_log:
    max_entries: 10000
    retention_hours: 12
    schedule: USING CRON */30 * * * * UTC
    schedule_cleanup: USING CRON 0 * * * * UTC
    is_disabled: false
    telemetry:
      - metrics
      - logs
      - biz_events
      - spans
